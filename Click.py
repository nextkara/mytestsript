#!/usr/bin/python
#python strip
echoDelay = "Delay 200"
echoTab = "KeyDown \"Tab\", 1"
echoTabUp = "KeyUp \"Tab\", 1"
echoEnter = "KeyDown \"Enter\", 1"
echoEnterUp = "KeyUp \"Enter\", 1"
echoSpace = "KeyDown \"Space\", 1"
echoSpace = "KeyUp \"Space\", 1"
outputFile = (r'IndexGUI.Q',w)
#inputFile = ('IndexGUI.java')
for Line in open('IndexGUI.java'):
    for x in Line:
        if x == '\\'
	  t = 1
	  continue
	if t == 1:
	  if x == 't'
	    i += 1
	  if x == 'r'
	    j += 1
def TabIn():
    outputFile.write(echoTab)
    outputFile.write(echoDelay)
    outputFile.write(echoTabUp)
    outputFile.write(echoDelay)
def EnterIn():
    outputFile.write(echoEnter)
    outputFile.write(echoDelay)
    outputFile.write(echoEnterUp)
    outputFile.write(echoDelay)
